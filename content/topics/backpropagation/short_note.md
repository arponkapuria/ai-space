Backpropagation is how a neural network figures out which weights caused how much of the final error, by working backward from the loss through each layer and multiplying local effects together using the chain rule. It's what makes training deep networks computationally feasible — one forward pass and one backward pass give you the gradient for every weight at once, instead of testing each weight one at a time.