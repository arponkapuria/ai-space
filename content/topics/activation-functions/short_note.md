An activation function is the nonlinear step applied after a neuron's weighted sum, and it's what lets a stack of layers learn curved, complex patterns instead of collapsing into one linear model. Which one you pick matters in practice: early choices like sigmoid caused gradients to vanish in deep networks, which is why ReLU and its smoother descendants like GELU became the default.